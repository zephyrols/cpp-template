#pragma once

#include <fmt/core.h>

namespace ExampleProject {
namespace utils {

class ExampleType {
private:
  // Private member variables and functions can be declared here

public:
  // Constructor
  void hello_world();
  int add(int a, int b);
};

} // namespace utils
} // namespace ExampleProject
